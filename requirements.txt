# Core dependencies are declared in pyproject.toml ([project.dependencies]).
# This file installs the package in editable mode and pulls those deps (single source of truth).
-e .

# Optional extras (same names as [project.optional-dependencies] in pyproject.toml):
#   pip install -e ".[dl]"        # torch — pick CUDA wheel separately; see README
#   pip install -e ".[h2l]"       # CReM
#   pip install -e ".[docking]"   # vina, meeko, gemmi
#   pip install -e ".[md]"        # openmm, pdbfixer, mdtraj (often easier via conda; see README)
#   pip install -e ".[lead_opt]"  # docking + md pip deps
#   pip install -e ".[all]"       # torch, crem, vina, meeko (+ gemmi via docking if extended)
#
# RDKit: conda install -c conda-forge rdkit
# REINVENT4: git clone + pip install -e . (see Makefile target install-reinvent4)
